# Default Workspace Template

`teams_runtime init` scaffolds a portable workspace from built-in defaults.

Runtime relay transport defaults to internal direct handoff.
Use `python -m teams_runtime start --relay-transport discord` when you need Discord relay-channel debugging.
